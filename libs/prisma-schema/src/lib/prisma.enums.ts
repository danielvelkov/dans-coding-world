import {
  PostStatus,
  PostVisibility,
  Role,
  ReportStatus,
} from '../generated/prisma/client.js';

export const PostStatusEnum = PostStatus;
export const PostVisibilityEnum = PostVisibility;
export const RoleEnum = Role;
export const ReportStatusEnum = ReportStatus;
