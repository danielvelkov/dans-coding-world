# svelte-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build svelte-lib` to build the library.

## Generators

### Svelte-lib

Run `nx g @dans-coding-world/svelte-lib:sveltelib [name] [directory]`
